    <div style="position:relative;height:60px">
         
        <label class="heading" style="margin-top:20px;margin-bottom:15px">My Favorite Properties</label>
        <hr>
{{#if savedProperties.length}}
    </div>

    {{#each savedProperties as |property|}}
        {{> favourites property}}
        </div>
    {{/each}}

{{else}}
<h3>you do not currently have any favorite properties</h3>

{{/if}}